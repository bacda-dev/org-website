/**
 * Wave 1 placeholder home.
 * Wave 2's frontend-dev agent replaces this by moving the home into
 * `app/(public)/page.tsx` (served at `/` via Next.js route groups) and
 * DELETES this file.
 */
export default function WaveOnePlaceholder() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6 py-24">
      <div className="max-w-2xl text-center">
        <p className="font-mono text-xs uppercase tracking-widest text-muted">
          Under construction
        </p>
        <h1 className="mt-6 font-display text-5xl font-medium md:text-6xl">
          Bay Area Creative Dancers
        </h1>
        <p className="mt-4 font-display text-xl italic text-burgundy md:text-2xl">
          ~ Foster the Love of Dance ~
        </p>
        <p className="mt-8 text-muted">
          The new site is being assembled. This placeholder confirms the
          foundation is live.
        </p>
      </div>
    </main>
  );
}
